import React from 'react';

const Inspect = () => {

    return (
        <>
            <p>
                First, you need to find the code to disable the alarm of the vault. To find the alarm code, you need to Hack and inspect the hell out of this vault! Find the code and type below.
            </p>
            <label for='code'>Type here the alarm code</label>
            <input id='code' type='text'></input>
        </>
    )
}

export default Inspect;